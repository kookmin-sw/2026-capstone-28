지금까지 진행된거

1. 객체 탐지 추출 (object_extractor -> MediaPipe 추출)  // 문제 코랩에서는 0.11.14 였는데 지금은 0.11.30 으로 최신 버전. -> 모델 추론 성능 하향 가능성
2. Motion BERT를 넣기 위해서 Torso 정규화 및 key_points 데이터 분포를 -1 ~ 1로 정규화 진행해야하는데
    코랩에서 진행 후 재 학습 , 추론해보니 기존 모델보다 성능이 떨어짐을 발견 -> 정확히 같은 지점을 제대로 찾지 못함
    * 추가로 기존 수정된 모델 가중치와 기존 데이터셋으로 추론해보니 성능이 더 떨어짐을 발견
    
    # 그래서 현 상태에서는 기존 모델 가중치를 기반으로 값 보정 후 결과 추출해야함. 
3. 현재까지 추론 결과물까지 제작됨. 추후 label 추출하는 것도 완성해서 model_output + label_output ->  LLM -> agent_output 으로 만들어야함.
4. 최종 result return은 model_output + agent_output으로 갈 거 같음. 
5. result에서 각 영상 시퀀스를 추출해야하고, 이를 영상에 매칭되게 파이프라인 제작해야함.  